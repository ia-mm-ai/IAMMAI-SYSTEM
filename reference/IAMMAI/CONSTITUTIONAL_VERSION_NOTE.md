# IAMMAI Constitutional Version Note

## 1. Purpose

This file clarifies the currently visible constitutional version surface and its rank.

Its job is to reduce ambiguity around the constitutional materials that are now visible in the repository, specifically:

- `protocol/IAMMAI_Public_Constitutional_Protocol_v1.0.pdf`
- `protocol/IAMMAI_Constitutional_Integrity_Protocol_v1.0.1.md`
- `protocol/canon.json`
- `protocol/protocol_identity.json`

It does not define:

- a rewrite of the PDF
- a rewrite of the markdown constitutional text
- a rewrite of `canon.json`
- a rewrite of `protocol_identity.json`
- a new constitutional protocol
- a standing declaration
- a manifesto
- a project-management note

This file is additive. It does not replace any constitutional surface. It exists so the visible version split can be read by rank rather than by ambiguity.

## 2. Visible Constitutional Surfaces

The currently visible constitutional surfaces are:

- `protocol/IAMMAI_Public_Constitutional_Protocol_v1.0.pdf`
  Public PDF publication surface.
- `protocol/IAMMAI_Constitutional_Integrity_Protocol_v1.0.1.md`
  Later derived markdown constitutional text surface.
- `protocol/canon.json`
  Machine canon surface at `v1.0.1`.
- `protocol/protocol_identity.json`
  Machine identity surface at `v1.0.1`.

These are visibly different surfaces by both file type and stated posture. They should not be read as one unranked bundle.

## 3. Present Ranking

The present ranked relationship is:

- Public publication surface:
  `protocol/IAMMAI_Public_Constitutional_Protocol_v1.0.pdf`
- Operative constitutional text surface for current repository reading:
  `protocol/IAMMAI_Constitutional_Integrity_Protocol_v1.0.1.md`
- Canon and protocol identity support surfaces:
  `protocol/canon.json`
  `protocol/protocol_identity.json`

This rank should not be read flatly.

The PDF is the preserved public constitutional artifact.

The markdown file is the current repository-native human constitutional text surface.

The JSON canon and identity files are derived machine surfaces and are subordinate to human constitutional text rather than peers of it.

## 4. Current Operative Reading

The current operative constitutional text surface for repository reading should be treated as:

- `protocol/IAMMAI_Constitutional_Integrity_Protocol_v1.0.1.md`

That file explicitly identifies itself as a derived markdown edition of the human-readable constitutional core, states that it was produced after the original PDF artifact to provide a cleaner machine-readable and repository-native reading surface, and states that the original PDF is preserved and not replaced by the markdown file.

`protocol/canon.json` and `protocol/protocol_identity.json` should be treated as machine canon and identity synchronization surfaces at `v1.0.1`, not as independent constitutional text surfaces. Their own fields state that they are derived from the human constitutional core and that human text overrides machine canon.

`protocol/IAMMAI_Public_Constitutional_Protocol_v1.0.pdf` should be treated as the preserved public publication surface and original constitutional artifact for the later derived markdown edition. It remains constitutionally important, but it is not the only visible surface now present in the repository.

## 5. Version Difference

The visible version split is real:

- the public PDF surface is visibly `v1.0`
- the markdown constitutional text surface is visibly `v1.0.1`
- the canon and protocol identity JSON surfaces are visibly `v1.0.1`

The narrowest reading supported by the visible repository state is:

- a later editorial increment on the repository-native human text surface
- matching canon and identity synchronization at `v1.0.1`
- while the public PDF surface remains visibly at the earlier `v1.0` publication version

The markdown file itself supports this narrow reading. It states that its change scope is title tightening, stylistic and readability brush-up, and markdown restructuring for legibility, and it explicitly states that it does not introduce new concepts, semantic changes, new constitutional claims, or altered protocol logic.

On the currently visible body, this version difference should not presently be read as demonstrated semantic contradiction. It is better read as a visible split between the earlier public publication surface and later derived `v1.0.1` repository-native and machine-support surfaces.

## 6. Reader Guidance

A first-time reader should treat the constitutional surfaces today by the following rule:

1. Read `protocol/IAMMAI_Constitutional_Integrity_Protocol_v1.0.1.md` as the current operative constitutional text surface for repository reading.
2. Read `protocol/IAMMAI_Public_Constitutional_Protocol_v1.0.pdf` as the preserved public constitutional artifact and original source text for that later markdown edition.
3. Read `protocol/canon.json` and `protocol/protocol_identity.json` as subordinate machine canon and identity surfaces that follow human constitutional text rather than outrank it.

That rule preserves rank without erasing the visible version split.

## 7. What This Does Not Claim

This file does not claim:

- that the PDF is invalid
- that the visible materials are semantically contradictory
- that the visible version split is fully resolved by this note
- that additive clarification replaces later explicit constitutional publication work

It also does not flatten publication surface, operative human text surface, and machine canon or identity surface into one undifferentiated constitutional object.

## 8. Closing Boundary Statement

This file clarifies the currently visible constitutional version surface.

It does not rewrite any constitutional artifact.

It exists to reduce ambiguity by additive rank clarification, not by retroactive smoothing.
