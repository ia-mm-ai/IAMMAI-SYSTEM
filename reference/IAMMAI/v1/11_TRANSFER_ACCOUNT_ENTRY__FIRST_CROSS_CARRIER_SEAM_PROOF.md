# IAMMAI v1 Transfer Account Entry: First Cross-Carrier Seam Proof

## 1. Purpose

This file records a new additive transfer-account entry for the first bounded cross-carrier seam proof now visible in the repository.

Its job is to place that threshold into explicit lineage without rewriting earlier account surfaces, without smoothing distinct proof sides into one generic transport event, and without inflating the result into a declaration of full relational field or multi-carrier body.

It does not define:

- a rewrite of `v1/06_V0_TO_V1_TRANSFER_ACCOUNT.md`
- a rewrite of `v1/07_TRANSFER_ACCOUNT_ENTRY__FIRST_PROOF_SLICES.md`
- a rewrite of `v1/08_TRANSFER_ACCOUNT_ENTRY__SHARED_IMPLEMENTATION_LADDERS.md`
- a rewrite of `v1/09_TRANSFER_ACCOUNT_ENTRY__PROOF_READABILITY_THRESHOLD.md`
- a rewrite of `v1/10_STANDING_REVIEW__CURRENT_THRESHOLD.md`
- a constitutional rewrite
- a middleware declaration
- a relational-field declaration
- a project-management diary
- a manifesto
- a README

This file is append-only. It follows earlier account objects and does not replace them.

## 2. Why This Entry Exists

The prior account and review objects remain standing:

- `v1/06_V0_TO_V1_TRANSFER_ACCOUNT.md` remains standing as transfer-account posture
- `v1/07_TRANSFER_ACCOUNT_ENTRY__FIRST_PROOF_SLICES.md` remains standing as the first proof-slice entry
- `v1/08_TRANSFER_ACCOUNT_ENTRY__SHARED_IMPLEMENTATION_LADDERS.md` remains standing as the shared-implementation threshold entry
- `v1/09_TRANSFER_ACCOUNT_ENTRY__PROOF_READABILITY_THRESHOLD.md` remains standing as the proof-readability threshold entry
- `v1/10_STANDING_REVIEW__CURRENT_THRESHOLD.md` remains standing as the current standing review

Those objects do not need retroactive revision in order for a new threshold to become visible.

A new executed threshold now exists under `lab/run_cross_carrier_seam_proof_001.py` and `lab/cross_carrier_seam_proof_001/`. It therefore needs its own bounded account entry rather than being folded backward into earlier text.

## 3. Cross-Carrier Threshold Executed

`lab/run_cross_carrier_seam_proof_001.py` now stands as the first bounded cross-carrier seam proof surface in the repository.

On the current visible archive, the preserved executed lineage is source-side and includes two bounded runs under `lab/cross_carrier_seam_proof_001/source_runs/`:

- `execution-71a0490222f8433e9b3081eecc817d3a` with `release`
- `execution-f72b3f6777fc47368c18567fa4df305e` with `release_invalid`

Both runs are bounded to the single artifact family `witness_artifact`.

This matters because cross-carrier seam is no longer present only as prose law or implementation intention. A bounded release surface now exists in executable lineage, with one valid carried package and one intentionally invalid carried package.

At the same time, the current visible body does not presently show preserved receiving-side runs under `lab/cross_carrier_seam_proof_001/receiving_runs/`. This entry therefore records the threshold exactly at the level the archive presently supports: executed source-side release lineage together with explicit ingress law and explicit refusal law on the carried package boundary.

## 4. Lawful Derivative Ingress

The lawful side of the proof is preserved by the valid release run `execution-71a0490222f8433e9b3081eecc817d3a`.

Its preserved summary and carried package keep the following conditions explicit:

- `expected_ingress_outcome = lawful_derivative_ingress`
- `package_ingress_lawful_if_received = true`
- `source_remains_source = true`
- `shared_authority = false`
- `standing_transferred = false`
- `derivative_only_arrival_rule = true`
- `standing_transfer_on_receipt = false`
- `standing_upgrade_requires_separate_ratification = true`

In architectural terms, that means the proof now preserves a lawful cross-carrier ingress posture in which arrival is derivative-only, source authority does not silently migrate, and receipt alone does not upgrade standing.

Because preserved receiving-side ingress artifacts are not presently visible in the archive, this entry records the lawful side in the form the lineage actually shows: a carried package whose ingress conditions remain lawful and explicit, rather than a silently assumed receiver-side inheritance event.

## 5. Visible Refusal / Non-Passage

The refusal side of the proof is preserved by the invalid release run `execution-f72b3f6777fc47368c18567fa4df305e`.

Its preserved summary, package, and package preflight conformance keep the failure visible:

- `expected_ingress_outcome = refusal_non_passage`
- `package_ingress_lawful_if_received = false`
- `package_attempted_unlawful_standing_transfer = true`
- package preflight conformance records `fail`

The bounded invalidity is also explicit. The invalid package:

- attempts standing transfer on receipt
- does not preserve derivative-only arrival on lawful ingress
- does not collapse failure into transport success

That matters because non-passage remains visible. The invalid transfer is not silently normalized, not silently upgraded into standing, and not treated as accepted merely because a package can be carried.

As with the lawful side, the current visible archive does not presently show preserved receiving-side refusal records. The present lineage therefore records refusal and non-passage explicitly at the carried-package and conformance boundary rather than pretending a receiver-side archive object already stands where it is not visible.

## 6. What This Now Proves

This threshold now proves, in bounded architectural terms, that:

- lawful cross-carrier release is now executable and preserved in lineage
- source legibility can survive across the carrier boundary
- derivative-only arrival can be preserved across the carrier boundary without silent authority inheritance
- visible refusal and non-passage can be preserved across the carrier boundary as explicit seam outcomes
- carrier crossing does not require shared authority, silent standing transfer, or hidden merge

More narrowly, the body has now shown that cross-carrier seam can be made operable as explicit release plus explicit ingress law, with lawful derivative-only arrival on the valid side and visible refusal on the invalid side.

## 7. What This Does Not Yet Prove

This entry does not yet prove:

- a full relational field
- a multi-carrier mesh
- bidirectional sync
- shared canonical state
- silent or automatic standing inheritance
- a full multi-carrier body
- total seam completion

It also does not claim more visible receiving-side execution lineage than the current archive actually preserves. On the current readable body, what stands is the first bounded cross-carrier seam proof surface, not a finished multi-carrier system.

## 8. Account Posture

This entry is additive.

It does not replace earlier account objects, and it does not retrospectively rewrite them to make the new threshold appear as though it had always already been recorded there.

The correct reading is cumulative:

- earlier transfer-account and review objects remain intact
- this new entry records the first cross-carrier seam-proof threshold now visible in lineage
- later threshold entries, if they become warranted, should follow the same additive posture rather than collapsing distinct threshold turns into retrospective smoothing

## 9. Closing Boundary Statement

This file records the first cross-carrier seam-proof threshold only.

It does not replace earlier account objects.

It exists so that the first bounded cross-carrier seam proof becomes visible in transfer-account lineage without retroactive rewrite, without flattening lawful derivative ingress and visible refusal into one transport event, and without overstating the result as full relational-field or multi-carrier completion.
