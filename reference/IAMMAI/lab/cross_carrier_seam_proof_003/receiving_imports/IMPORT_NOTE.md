# Cross-Carrier Seam Proof 003 Receiving Import Note

## 1. Purpose

This note preserves readability for the imported receiving-side evidence under `lab/cross_carrier_seam_proof_003/receiving_imports/`.

It does not rewrite any lab artifact, replace any underlying run record, or convert imported receiving-side material into source-side lineage.

## 2. Imported Receiving-Side Evidence

The materials preserved here are receiving-side evidence from the third cross-carrier seam proof.

These runs were executed on the receiving carrier and were later imported back into the main body as additive evidence for the receiving side of the proof.

## 3. Rank / Relation

Source-side release lineage under `source_runs/` remains source-side release lineage.

Receiving-side ingress lineage under `receiving_imports/` remains receiving-side lineage.

Import preserves relation between those sides without merging them into one local event or flattening receiving-side evidence into source-side history.

## 4. Imported Branches

`clean/` preserves the receiving-side branch in which the same valid package was read under an admissible receiver-local condition and lawful derivative-only ingress was admitted.

`contaminated_non_passage/` preserves the receiving-side branch in which the same valid package was technically received under a contaminated receiver-local condition and lawful ingress was denied with visible non-passage.

These branches remain distinct receiving-side outcomes inside the proof.

## 5. Boundary Statement

This note exists only to preserve readability of imported receiving-side evidence within the third cross-carrier seam proof.

It does not replace the underlying imported run artifacts.
