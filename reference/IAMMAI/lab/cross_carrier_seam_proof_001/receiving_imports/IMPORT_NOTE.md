# IAMMAI Cross-Carrier Seam Proof 001 Receiving Import Note

## 1. Purpose

This note preserves the readability of the imported receiving-side evidence under `lab/cross_carrier_seam_proof_001/receiving_imports/`.

It exists only to clarify rank, relation, and branch distinction for the imported materials. It does not rewrite any lab artifact, it does not function as a transfer-account entry, and it does not replace the underlying run artifacts.

## 2. Imported Receiving-Side Evidence

The materials under `receiving_imports/` are receiving-side evidence from the first cross-carrier seam proof.

They were produced on the receiving carrier and later imported back into the main body as additive evidence. Their preserved summaries remain receiving-side summaries, including `mode = ingress` and original run-directory references under `lab/cross_carrier_seam_proof_001/receiving_runs/`.

## 3. Rank / Relation

Source-side release lineage under `source_runs/` and receiving-side ingress lineage under `receiving_imports/` remain distinct.

Import preserves relation between the two sides without merging the imported materials into source-side release lineage and without flattening source-side release and receiving-side ingress into one local event.

## 4. Imported Branches

- `lawful/` preserves the receiving-side lawful derivative-ingress branch. On the current imported evidence, this branch records lawful ingress with derivative-only arrival, `source_remains_source = true`, `shared_authority = false`, and `standing_upgraded = false`.
- `refusal_non_passage/` preserves the receiving-side refusal and non-passage branch. On the current imported evidence, this branch records `ingress_lawful = false`, visible refusal, non-passage, and no silent upgrade of unlawful transfer into standing.

These branches remain distinct receiving-side outcomes.

## 5. Boundary Statement

This note exists only to preserve the readability of imported receiving-side evidence within the first cross-carrier seam proof.

It does not replace the imported run artifacts, and it does not alter their receiving-side status by the fact of import.
