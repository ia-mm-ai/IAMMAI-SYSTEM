# Cross-Carrier Seam Proof 004: Receiving Imports Note

## 1. Purpose

This note preserves readability for the imported receiving-side evidence under `receiving_imports/`.

It does not rewrite any lab artifact, replace any run record, or convert imported receiving-side evidence into source-side release lineage.

## 2. Imported Receiving-Side Evidence

The materials under `receiving_imports/` are receiving-side evidence from the fourth cross-carrier seam proof.

These runs were executed on the receiving carrier and were later imported back into the main body as additive evidence for bounded receiving-side ingress and arrival.

## 3. Rank / Relation

Source-side release lineage under `source_runs/` and receiving-side ingress lineage under `receiving_imports/` remain distinct.

Import preserves relation without flattening source-side release and receiving-side arrival into one local event. The imported materials remain receiving-side evidence after import.

## 4. Imported Branches

`clean/` preserves the receiving-side bounded in-between arrival produced from the clean-origin comparison package.

`obsolete_origin/` preserves the receiving-side bounded in-between arrival produced from the obsolete-origin release package in which withheld origin approval was not treated as lawful veto.

These branches remain distinct receiving-side outcomes. They are not merged into one generic arrival record.

## 5. Boundary Statement

This note exists only to preserve readability of imported receiving-side evidence within the fourth cross-carrier seam proof.

It does not replace the underlying run artifacts.
