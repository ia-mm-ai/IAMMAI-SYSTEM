{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "IAMMAI Current-State / Standing Reader v1 Input Packet",
  "description": "Input packet for current_state_standing_reader_v1 only. Source access is governed by vessel/current_state_standing_reader_v1/source_manifest.json. This packet does not permit the caller to widen scope, authority, or source access.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contract_id",
    "request_id",
    "question_class",
    "question_text"
  ],
  "properties": {
    "contract_id": {
      "type": "string",
      "const": "current_state_standing_reader_v1",
      "description": "Fixed contract identifier for this vessel only."
    },
    "request_id": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[A-Za-z0-9._:-]+$",
      "description": "Bounded identifier for one vessel request."
    },
    "question_class": {
      "type": "string",
      "enum": [
        "current_state",
        "current_governing_surfaces",
        "what_stands_now",
        "latest_thresholds",
        "what_remains_open",
        "where_to_read_next"
      ],
      "description": "Allowed question class for this vessel. Any class outside this set is out of scope."
    },
    "question_text": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Bounded natural-language request for derivative reading only."
    },
    "context_note": {
      "type": [
        "string",
        "null"
      ],
      "maxLength": 500,
      "description": "Optional bounded operator note. It does not widen source access, authority, or question scope."
    }
  },
  "examples": [
    {
      "contract_id": "current_state_standing_reader_v1",
      "request_id": "req_001",
      "question_class": "current_state",
      "question_text": "What stands now?",
      "context_note": "Optional bounded operator note"
    }
  ]
}
