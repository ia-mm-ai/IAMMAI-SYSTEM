# Portable Source-Body Verification Command Implementation V0 Minimum Specification

## 1. Purpose

This file defines the future portable source-body verification command implementation specification for the present `IAMMAI-SYSTEM` execution line.

This is the next lawful mechanical gate after `PORTABLE_SOURCE_BODY_VERIFICATION_COMMAND_IMPLEMENTATION_BOUNDARY_TERMINAL_SUMMARY_V0.md`.

The purpose is to define what a future checker-only command implementation may look like if separately authorized later.

This file does not implement the command.

This file does not create code.

This file does not create a script.

This file does not create a CLI.

This file does not create command invocation.

This file does not create command output.

This file does not create command result.

This file does not create command success.

This file does not execute anything.

This file does not run tests.

This file does not authorize command execution.

This file does not authorize live verification.

This file may define future implementation conditions only, including a future implementation file path candidate, checker-only function surface, reference-shaped input requirement, bounded report shape, no live command execution, no command output as source, no command success as currentness, no command success as final completion, no artifact mutation, no full prior artifact embedding, and command execution requiring a separate boundary.

These are future implementation specification conditions only.

They do not create command implementation.

They do not authorize immediate command implementation.

They do not authorize command execution.

The future portable source-body verification command implementation, if later separately authorized, must be checker-only. It may inspect declared evidence references and produce a bounded non-authoritative report shape. It must not create command execution, command invocation, command success, live verification standing, source, authority, currentness, operation permission, public readiness, final completion, continuation, reusable permission, deployment, runtime hosting, public release, derivative reception, vessel relation, another reception request, or follow-on work.

This file is command implementation specification only. It is not command implementation, command execution, command invocation, command output, command result, command success, script, CLI, test, resolver, artifact JSON, manifest implementation, checksum implementation, signature implementation, packet implementation, deployment, runtime hosting, public release, source transfer, source migration, source receipt, reception authorization, final completion, README, roadmap, or manifesto.

## 2. Status and Rank

This specification is additive.

This specification ranks below constitutional and reference authority surfaces, including the preserved `reference/IAMMAI/` surfaces.

This specification does not replace `PORTABLE_SOURCE_BODY_VERIFICATION_COMMAND_IMPLEMENTATION_BOUNDARY_TERMINAL_SUMMARY_V0.md`.

This specification does not replace `PORTABLE_SOURCE_BODY_VERIFICATION_COMMAND_IMPLEMENTATION_BOUNDARY_V0_MIN_SPEC.md`.

This specification does not replace the command implementation-boundary resolver, tests, or live artifact.

This specification does not replace command-boundary terminal summary.

This specification does not replace artifact emission containment.

This specification does not replace evidence-manifest.

This specification does not create command implementation.

This specification does not authorize command execution.

This specification does not create command invocation.

This specification does not create command output.

This specification does not create command result.

This specification does not create command success.

This specification does not create source.

This specification does not create authority.

This specification does not create currentness.

This specification does not create permission.

This specification does not create public readiness.

This specification does not create final completion.

This specification does not create continuation.

This specification does not authorize follow-on work.

This specification does not create manifest implementation, checksum implementation, signature implementation, packet implementation, deployment, runtime hosting, public release, source transfer, source migration, source receipt, reception authorization, operation permission, reusable permission, derivative reception, vessel relation, another reception request, or publication flow.

## 3. Why This Spec Is Needed Now

Portable source-body verification recorded carrier-independent verification.

Evidence-manifest recorded checkable evidence classes.

Artifact emission containment required reference-shaped selected basis.

Command boundary recorded checker-only command posture.

Command implementation-boundary recorded conditions for future implementation.

Command implementation-boundary terminal summary preserved that no command exists and no command execution is authorized.

The next mechanical pressure is to specify a future command implementation shape without creating it.

Without this specification, implementation-shaped work could collapse into command execution, command output authority, command success currentness, deployment readiness, public readiness, final completion, continuation, reusable permission, or follow-on authorization.

This specification keeps future implementation shape distinct from implementation, execution, invocation, output, result, success, source, authority, currentness, operation permission, deployment, runtime hosting, public release, public readiness, final completion, continuation, reusable permission, derivative reception, vessel relation, another reception request, and follow-on work.

## 4. Definitions

`portable_source_body_verification_command_implementation` means a possible future code implementation of a checker-only portable verification command. In this specification, implementation means code may exist later only if separately authorized.

`checker_only_implementation` means a future implementation may only inspect declared evidence references and build bounded reportable findings if separately authorized. It may not create execution, invocation, output standing, result authority, success currentness, source, authority, currentness, permission, public readiness, final completion, continuation, reusable permission, deployment, runtime hosting, public release, derivative reception, vessel relation, another reception request, or follow-on work.

`implementation_target_candidate` means a future file path candidate named by this specification. It is not a created file, command implementation, script, CLI, invocation, output, result, or success.

`implementation_module` means a future Python module that may exist only if separately authorized by a later prompt and boundary. It is not executable permission.

`command_function_surface` means the possible future pure helper function names and call signatures exposed by the implementation module for bounded report building. Function surface language in this file is not implementation.

`bounded_report_shape` means a future dictionary shape that may hold non-authoritative checker findings. It is not source, authority, currentness, final completion, operation permission, public readiness, continuation, reusable permission, or follow-on authorization.

`reportable_finding` means a bounded finding that a future checker-only implementation may state about declared evidence if separately authorized and supplied with reference-shaped input. A reportable finding is not source, authority, currentness, final completion, or permission.

`reference_shaped_input` means prior results are represented by id, path, outcome, check counts, summary, non-claims, basis reference, artifact family, and artifact size class, not by full nested artifact bodies.

`declared_evidence_reference` means a bounded reference to evidence selected for checking. It does not become source, authority, currentness, permission, deployment readiness, public readiness, final completion, or follow-on work.

`selected_result_reference` means a reference-shaped representation of one selected prior result.

`full_prior_artifact_body` means the full raw content of an upstream artifact, including nested basis objects, checks, statements, summaries, non-claims, blocks, and any embedded prior artifacts.

`live_command_execution` means actually running a command or causing the command to inspect live inputs as an execution event. Live command execution remains future and requires a separate execution boundary.

`command_invocation` means a shell call, CLI call, script call, workflow call, function call treated as operational command run, or comparable trigger that runs the command. This specification does not create invocation.

`command_output` means data emitted by a command after execution. This specification does not create command output. A report shape is not output from a live command unless execution is separately authorized.

`command_result` means a future report object or comparable result produced by a separately implemented and separately executed command. This specification does not create a command result.

`command_success` means a future command's successful exit, passed checks, or comparable execution success. Command success must not create source, authority, currentness, operation permission, public readiness, final completion, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.

`command_output_non_authority` means future command output, if any, must remain non-authoritative and must not become source, authority, currentness, final completion, permission, public readiness, continuation, reusable permission, or follow-on authorization.

`command_success_non_currentness` means future command success must not make a path, repository, carrier, artifact, timestamp, environment, device, operating system, vendor account, local clone, or narration current.

`command_success_non_final_completion` means future command success must not claim final completion, public readiness, operation permission, continuation, reusable permission, publication flow, another reception request, or follow-on work.

`implementation_without_execution` means code shape may be specified, and later may be created if separately authorized, without permission to run the command.

`execution_boundary_dependency` means live command invocation, output, result, and success require a separate future execution boundary and cannot be inferred from this specification or any future implementation file.

Implementation is not execution.

Report shape is not output from a live command unless execution is separately authorized.

Reportable finding is not source, authority, currentness, final completion, or permission.

Reference-shaped input means selected prior results are represented by id, path, outcome, check-count, summary, and non-claims rather than full nested artifacts.

## 5. Future Implementation Target

The future implementation target candidate is:

`src/portable_source_body_verification_command.py`

This specification does not create that file.

This specification does not authorize Codex to create that file yet.

This specification only defines the future target if later separately authorized.

No CLI wrapper is created here.

No `scripts/` entrypoint is created here.

No shell command is created here.

No command invocation is created here.

No command output, command result, command success, runtime surface, deployment surface, public release surface, or package configuration is created here.

## 6. Allowed Future Module Role

A future implementation module, if separately authorized, may only:

- accept declared reference-shaped evidence inputs
- validate required reference fields
- validate required non-claims are false
- validate evidence-manifest dependency is declared
- validate command-boundary and implementation-boundary dependencies are declared
- validate artifact emission containment dependency is declared
- validate no full prior artifact bodies are embedded
- build a bounded report dictionary
- build a bounded summary dictionary
- optionally write a bounded non-authoritative report artifact only if a later boundary authorizes writing
- expose pure helper functions for tests

Future module behavior may not execute live verification unless separately authorized.

Future module behavior may not run shell commands.

Future module behavior may not run a test suite.

Future module behavior may not mutate artifacts.

Future module behavior may not infer currentness from path, timestamp, latest-file posture, carrier, repository, artifact existence, or narration.

Future module behavior may not treat a local path, repository clone, artifact root, carrier possession, operating system, vendor environment, account, hidden local state, or human narration as source, authority, currentness, permission, public readiness, final completion, continuation, reusable permission, or follow-on authorization.

## 7. Prohibited Future Module Behavior

A future implementation must not:

- run automatically on import
- call subprocess
- run tests
- invoke shell commands
- call network
- call OpenAI or external APIs
- create CLI behavior
- create command invocation
- create command output as standing result
- treat command output as source
- treat command output as authority
- treat command success as currentness
- treat command success as final completion
- embed full prior artifacts
- mutate artifacts
- compact artifacts
- delete artifacts
- rewrite artifacts
- normalize artifacts
- repair artifacts
- invalidate artifacts by size
- create manifest implementation
- create checksum implementation
- create signature implementation
- create packet implementation
- create deployment
- create runtime hosting
- create public release
- authorize source transfer
- authorize source migration
- authorize source receipt
- authorize reception
- authorize continuation
- authorize reusable permission
- authorize derivative reception
- authorize vessel relation
- authorize another reception request
- authorize follow-on work

Prohibited behavior remains prohibited even if a future implementation is separately authorized.

Command execution requires a separate future boundary.

## 8. Required Future Inputs

A future implementation, if separately authorized, must require reference-shaped input classes including:

- selected command implementation-boundary reference
- selected command-boundary reference
- selected artifact emission containment reference
- selected evidence-manifest reference
- selected portable verification reference
- selected source-body reception terminal summary reference
- declared evidence references
- required source surface references
- required spec surface references
- required resolver surface references
- required test surface references
- required artifact root references
- required closure artifact references
- declared non-claims
- execution non-authorization posture

Each input must be reference-shaped.

Full artifact bodies are not required.

Full artifact bodies must not be embedded.

Input existence, input path readability, artifact presence, latest-file posture, repository possession, carrier possession, environment state, or narration must not create source, authority, currentness, permission, command execution, command output, command success, deployment readiness, public readiness, final completion, continuation, reusable permission, or follow-on authorization.

## 9. Reference-Shaped Input Contract

Selected result references should use these fields:

- `selected_result_id`
- `selected_result_path`
- `selected_result_outcome`
- `selected_result_failed_check_count`
- `selected_result_passed_check_count`
- `selected_result_summary`
- `selected_result_non_claims`
- `selected_result_basis_reference`
- `selected_result_artifact_family`
- `selected_result_artifact_size_class`

`selected_result_path` is reference only.

Artifact existence is not currentness.

Artifact size class is descriptive only.

Summary is not source.

Selected non-claims preserve anti-collapse posture.

Missing reference may produce bounded refusal, blocked review, or insufficient basis if later implemented.

Missing reference does not create authority.

Reference records do not replace prior artifacts.

Reference records do not mutate prior artifacts.

Reference records do not compact, delete, rewrite, normalize, repair, migrate, truncate, or invalidate prior artifacts.

## 10. Future Function Surface

Possible future public functions, if later separately authorized, are:

- `build_portable_verification_command_report(request: Mapping[str, Any]) -> dict`
- `build_portable_verification_command_summary(report: Mapping[str, Any]) -> dict`
- `write_portable_verification_command_report(report: Mapping[str, Any], output_path: Path | str | None = None) -> Path`

These are future function names only.

This specification does not implement them.

Report building is not live command execution.

Report building is not command invocation.

Report building is not command output from a live run.

Write behavior, if later authorized, must be additive and contained.

No function may mutate upstream artifacts.

No function may treat a report as source, authority, currentness, final completion, public readiness, operation permission, continuation, reusable permission, or follow-on authorization.

No function may run automatically on import.

No function may call subprocess, network, OpenAI, external APIs, shell commands, package installers, test runners, deployment tools, runtime hosts, or public release surfaces.

## 11. Future Report Shape

A possible future report may use these sections:

- `portable_source_body_verification_command_report_metadata`
- `declared_command_report_question`
- `selected_command_implementation_boundary_reference`
- `selected_command_boundary_reference`
- `selected_artifact_emission_containment_reference`
- `selected_evidence_manifest_reference`
- `selected_portable_verification_reference`
- `declared_evidence_references`
- `reference_shape_checks`
- `non_claim_checks`
- `checker_findings`
- `bounded_report_statement`
- `report_non_meaning`
- `what_remains_open`
- `non_claims`
- `status`
- `block`

Future report status is not command success.

Future report status is not final completion.

Future report is not source.

Future report is not authority.

Future report does not create currentness.

Future report does not authorize execution.

Future report does not authorize deployment, runtime hosting, public release, operation permission, public readiness, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.

This specification does not create a report artifact.

## 12. Future Status Family

A future report-building implementation may use this status family:

- `PORTABLE_VERIFICATION_COMMAND_REPORT_BUILT`
- `PORTABLE_VERIFICATION_COMMAND_REPORT_NOT_BUILT`
- `PORTABLE_VERIFICATION_COMMAND_REPORT_REQUIRES_ADDITIONAL_BASIS`
- `PORTABLE_VERIFICATION_COMMAND_REPORT_BLOCKED`

`PORTABLE_VERIFICATION_COMMAND_REPORT_BUILT` means a bounded report object was built. It does not mean command success.

`PORTABLE_VERIFICATION_COMMAND_REPORT_NOT_BUILT` means a readable request failed report-building review.

`PORTABLE_VERIFICATION_COMMAND_REPORT_REQUIRES_ADDITIONAL_BASIS` means more basis is needed.

`PORTABLE_VERIFICATION_COMMAND_REPORT_BLOCKED` means the request is missing, malformed, or overreach-shaped.

None of these statuses authorizes command execution.

None of these statuses creates command invocation.

None of these statuses creates command output as source.

None of these statuses creates command result authority.

None of these statuses creates command success.

None creates final completion, currentness, public readiness, operation permission, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.

## 13. Required Future Checks

A future implementation may check only:

- reference-shaped input present
- required selected result references present
- expected outcomes declared
- failed check counts are zero where required
- non-claims are false
- no full prior artifact body embedded
- artifact emission containment reference present
- evidence-manifest reference present
- command-boundary reference present
- implementation-boundary reference present
- command execution not authorized
- command output not created as source
- command success not created
- path does not create currentness
- artifact existence does not create currentness
- report remains non-authoritative

These checks are checker-only report-building checks.

They do not create source, authority, currentness, permission, public readiness, final completion, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.

These checks must not mutate, compact, delete, rewrite, normalize, repair, migrate, replace, truncate, or invalidate artifacts.

These checks must not embed full prior artifacts.

These checks must not run shell commands, tests, subprocesses, package managers, network calls, OpenAI calls, deployment tools, runtime hosts, or public release surfaces.

## 14. Blocking Conditions for Future Implementation

Future report-building must block when:

- request missing
- request malformed
- required reference missing
- required outcome missing
- required failed check count nonzero
- required non-claim missing or flipped
- full prior artifact body embedded
- artifact mutation requested
- command execution requested
- command invocation requested
- command output treated as source
- report treated as authority
- command success treated as currentness
- command success treated as final completion
- deployment requested
- runtime hosting requested
- public release requested
- continuation requested
- follow-on requested

Blocked report-building does not mutate prior artifacts.

Blocked report-building does not repair input.

Blocked report-building does not authorize next work.

Blocked report-building does not create command implementation, command execution, command invocation, command output, command result, command success, source, authority, currentness, public readiness, final completion, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.

## 15. Relation to Command Implementation-Boundary

Command implementation-boundary made this implementation specification inspectable.

Command implementation-boundary recorded implementation conditions only.

Command implementation-boundary did not authorize implementation.

Command implementation-boundary did not authorize execution.

This specification does not mutate the implementation-boundary result.

This specification does not replace the implementation-boundary result.

This specification must not overread implementation-boundary as command implementation permission.

Any future implementation still requires a separate Codex prompt and explicit one-file scope.

Any future implementation must preserve the implementation-boundary conditions that the command remains checker-only, contained-reference-shaped, non-authoritative, non-currentness-creating, non-final-completion-claiming, and execution-separated.

## 16. Relation to Future Execution Boundary

Even after implementation, execution remains future work.

Command execution boundary must come before live command invocation.

No implementation specification may be treated as permission to run the command.

No implementation result may be treated as permission to run the command.

No implementation file may be treated as permission to run the command.

Command output remains separately bounded.

Command result remains separately bounded.

Command success remains separately bounded.

Command output must not become source or authority.

Command success must not become currentness or final completion.

Execution must not be inferred from command existence, path existence, repository possession, artifact existence, latest-file posture, local environment, test availability, shell availability, user narration, or implementation completion.

## 17. What Remains Open

These remain open and not executed:

- actual command implementation file
- command implementation tests
- command report artifact
- command execution boundary
- command invocation
- command output
- command result
- command success
- manifest implementation
- checksum implementation
- signature implementation
- source-body packet implementation
- reproducible environment declaration
- runtime hosting
- deployment
- public release
- source transfer
- source migration
- source receipt
- reception authorization
- derivative reception
- vessel relation
- adoption
- authority creation
- currentness creation
- operation permission
- public readiness
- final completion
- continuation
- publication flow
- reusable permission
- successor reception request
- follow-on work

Open means not scheduled.

Open means not authorized.

Open means not executed.

## 18. Preserved Non-Claims

The following false posture is preserved:

- `command_implemented = false`
- `command_executed = false`
- `command_authorized_to_run = false`
- `command_invocation_created = false`
- `command_output_created = false`
- `command_result_created = false`
- `command_success_created = false`
- `command_output_became_source = false`
- `command_output_became_authority = false`
- `command_success_created_currentness = false`
- `command_success_claimed_final_completion = false`
- `command_became_authority = false`
- `full_prior_artifacts_embedded = false`
- `prior_artifacts_mutated = false`
- `manifest_implemented = false`
- `checksum_implemented = false`
- `signature_implemented = false`
- `packet_implemented = false`
- `deployment_created = false`
- `runtime_hosting_created = false`
- `public_release_created = false`
- `operation_permission_created = false`
- `public_launch_readiness_created = false`
- `final_completion_claimed = false`
- `continuation_authorized = false`
- `publication_flow_opened = false`
- `reusable_permission_created = false`
- `derivative_reception_authorized = false`
- `vessel_relation_authorized = false`
- `another_reception_request_authorized = false`
- `follow_on_work_authorized = false`

These non-claims remain false even if a future command implementation file is separately authorized and created.

These non-claims remain false even if a future report shape is separately authorized and built.

Command execution remains separately bounded.

## 19. Closing Statement

Portable source-body verification command implementation specification may define the future checker-only command module shape. It preserves that this specification is not command implementation, command execution, command invocation, command output, command result, command success, manifest implementation, checksum implementation, signature implementation, packet implementation, deployment, runtime hosting, public release, operation permission, public readiness, final completion, continuation, reusable permission, derivative reception, vessel relation, another reception request, or follow-on work.
